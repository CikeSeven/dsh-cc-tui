# pi-tui fork patch ledger (WP-03)

Every non-mechanical local difference from the pinned upstream snapshot
(`086c32e74530564922d011ade23ff582c9d63116`, `@earendil-works/pi-tui` 0.84.2) must be a row
in the table below. Mechanical transformations performed by
`scripts/vendor-pi-tui.mjs` are recorded as row(s) with kind = mechanical.
Re-vendor steps column names the exact command that reapplies the fork state.

| 文件 | 上游行为 | DSH 改动 | 原因 | 回归测试 | re-vendor 步骤 |
| --- | --- | --- | --- | --- | --- |
| mechanical: `src/tui-v2/vendor/pi-tui/src/**/*.ts` | 上游相对路径 import/export specifier 带 `.ts` 后缀（strip-types 风格） | 机械改写为 `.js` 后缀（bundler resolution；内容由 `scripts/vendor-pi-tui.mjs` 在复制时转换） | 本仓库 tsconfig 用 bundler resolution + `tsc` 输出，运行 import 必须 `.js`；不计行为 patch | `pnpm compile` + `node scripts/test-tui-v2.mjs`（`test/tui-v2/pi-fork/` 全部上游移植测试） | `node scripts/vendor-pi-tui.mjs --source <pi checkout>`（重写自动应用，无需手工步骤） |
| fork-added: `src/tui-v2/vendor/pi-tui/src/dsh/pi-output-encoder.ts` | 无此文件 | 新增 PiOutputEncoder typed builder 集：sync output、alt screen、autowrap、mouse 组合、cursor 显隐、ED/EL/ECH 擦除、CUU/CUD/CHA/CUP、CR/CRLF、SGR reset、segmentReset、2031 通知、cell-size/background-color/color-scheme query、OSC 52 clipboard；每个 builder 产出与上游字面量逐字节相同的字符串 | WP-03c（计划 §5.6）：fork 的 Tui/main/alt screen 不得再手写 ANSI 字面量后调用 write，control plane 一律走 typed builder；builder 输出是 adapter 端 `parsePiTerminalString` 的 allowlist 来源 | `test/tui-v2/terminal-pi-adapter.test.ts`（encoder→parse round-trip fixture）+ `test/tui-v2/pi-fork/` 上游测试（字节不变性） | re-vendor 后保留本文件（vendor 脚本只清理上游 closure 路径，`src/tui-v2/vendor/pi-tui/src/dsh/` 不受影响），`node scripts/vendor-pi-tui.mjs --record-fork-patch` 重记 hash |
| fork-added: `src/tui-v2/vendor/pi-tui/src/dsh/pi-string-parser.ts` | 无此文件 | 新增 `parsePiTerminalString` 严格 parser：只接受 encoder 产出的 control-plane 形态 + data plane（可打印文本/受控换行/SGR/OSC 8）+ 已登记 query + 已声明 kitty/iTerm2 image marker；未知 CSI/DEC/OSC、未登记 APC（含 `_pi:c` cursor marker）、未闭合 OSC、超 8 MiB payload 一律拒绝 `unsupported-pi-sequence`；partial 尾巴以 remainder 返回由 adapter 重组 | WP-03c（计划 §5.6）：`PiTerminalAdapter.write(string)` 兼容边界的唯一解析器，未知序列不得原样透传；纯函数、vendor 内自包含（只依赖 node Buffer） | `test/tui-v2/terminal-pi-adapter.test.ts`（round-trip / 未知序列拒绝 / partial 重组 / 超长 payload fixture） | 同上一行（保留文件 + `--record-fork-patch`） |
| `src/tui-v2/vendor/pi-tui/src/tui.ts` | `TuiBase.start/stop/setTerminalColorSchemeNotifications/queryCellSize/queryTerminalBackgroundColor/queryTerminalColorScheme` 直接拼接 `"\x1b[?2031h"`、`"\x1b[16t"`、`"\x1b]11;?\x07"`、`"\x1b[?996n"` 等字面量调用 `terminal.write`；`SEGMENT_RESET` 为手写字面量 | 上述调用点全部改用 `./dsh/pi-output-encoder.js` builder（`setColorSchemeNotifications`/`queryCellSize`/`queryBackgroundColor`/`queryColorScheme`/`segmentReset`），产出字节与上游完全一致；行为不变 | WP-03c control plane 走 encoder 的硬性规则；2031/16t/OSC11/996n 属于已登记 query/通知序列 | `test/tui-v2/pi-fork/`（terminal-colors、tui-cell-size-input 等上游测试）+ `test/tui-v2/terminal-pi-adapter.test.ts` round-trip | re-vendor 后手工重放本行 6 处替换（见当前 git diff），再 `--record-fork-patch` |
| `src/tui-v2/vendor/pi-tui/src/tui-main-screen.ts` | `doRender`/`beforeTerminalStop`/`positionHardwareCursor` 手写 `"\x1b[?2026h"`、`"\x1b[2J\x1b[H\x1b[3J"`、`` `\x1b[${n}B` ``、`"\r\n\x1b[2K"`、`` `\x1b[${col}G` `` 等拼 buffer | 全部改用 `piOutput.*` builder（syncOutputBegin/End、clearScreenHomeScrollback、cursorUp/Down、cursorColumn、carriageReturn、newline、eraseLine），字节逐一相同；行内容（含 SGR/OSC 8/kitty marker 的 data plane）保持原样插值 | 同上（control plane 走 encoder）；main-screen 的 append-only/live-region 语义不变 | `test/tui-v2/pi-fork/tui-render.test.ts`、`test/tui-v2/pi-fork/tui-shrink.test.ts` 等上游测试 + `test/tui-v2/pi-fork-integration.test.ts` 主屏端到端 | re-vendor 后手工重放本行改动，再 `--record-fork-patch` |
| `src/tui-v2/vendor/pi-tui/src/tui-alt-screen.ts` | 顶部常量 `ENTER_ALT_SCREEN`/`DISABLE_AUTOWRAP`/mouse 组合/`BEGIN_SYNCHRONIZED_OUTPUT` 等手写字面量；`beforeTerminalStart`/`beforeTerminalStop`/`afterTerminalStop`/`doRender`/`copySelectionToClipboard` 拼接写 buffer | 输出常量删除，调用点改用 `piOutput.*` builder（enterAltScreen/exitAltScreen、setAutowrap、enableButtonMotionMouse/enableAllMotionMouse/disableMouse、eraseDisplay、cursorHome、cursorTo、eraseLine、setCursorVisible、sgrReset、osc52Clipboard），字节逐一相同；`FOCUS_IN`/`FOCUS_OUT`/mouse 正则/OSC133 strip 属输入解析侧保留；search/selection 高亮 SGR（`\x1b[7m` 等）属 data plane 行内容保留 | 同上；OSC 52 属已声明 clipboard 序列，adapter parser 登记接受 | `test/tui-v2/pi-fork/tui-alt-screen.test.ts`（mouse/OSC52/selection 上游断言）+ `test/tui-v2/pi-fork-integration.test.ts` alt 屏/overlay 端到端 | re-vendor 后手工重放本行改动，再 `--record-fork-patch` |
| data-plane 边界说明（`src/tui-v2/vendor/pi-tui/src/tui.ts`、`src/tui-v2/vendor/pi-tui/src/tui-alt-screen.ts`、`src/tui-v2/vendor/pi-tui/src/terminal-image.ts`、`src/tui-v2/vendor/pi-tui/src/components/`） | 组件渲染产出的行内容含 SGR 样式、OSC 8 超链接、kitty/iTerm2 image marker（`terminal-image.ts` 的 `encodeKitty`/`encodeITerm2`/`deleteKittyImage` 等返回原始序列字符串） | 不改：data plane（行内容 SGR/OSC 8）与已声明 image marker 由 `parsePiTerminalString` 的对应规则接受；用户/工具文本在 v2 数据面另有 escape encoder（WP-05），不复用此 parser；`ProcessTerminal`（`src/tui-v2/vendor/pi-tui/src/terminal.ts`）是上游参考实现，dsh 运行时不使用（由 `PiTerminalAdapter` 替代），保持 pristine | 明确 control plane（cursor/erase/mode/scroll/title/progress/image/query）与 data plane（行内容）边界，避免误伤组件生态 | `test/tui-v2/pi-fork/` 全部上游测试 + `test/tui-v2/terminal-pi-adapter.test.ts` 的 image/OSC52/round-trip fixture | 无（说明行） |
